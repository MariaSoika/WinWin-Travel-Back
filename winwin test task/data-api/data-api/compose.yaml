server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/test
    username: postgres
    password: 27062006
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

internal:
  token: shared-secret
